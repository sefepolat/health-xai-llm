# requirements.docker.txt
# -----------------------
# Docker image için bağımlılıklar — Python 3.12 uyumlu, CPU-only.
#
# Neden CPU-only torch?
#   sentence-transformers tam torch'u çekiyor → CUDA paketleriyle ~8GB image.
#   Bu proje embedding için CPU kullanıyor (model küçük, CPU yeterli).
#   --extra-index-url ile PyTorch'un CPU wheel deposunu gösteriyoruz.
#   CPU torch ~250MB, CUDA torch ~2GB+.

# ---------------------------------------------------------------------------
# Önce torch CPU-only kur — diğer paketler bunu dependency olarak görecek
# ---------------------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2

# --- Veri İşleme ---
numpy>=2.0
pandas>=2.0
scipy>=1.10
pyarrow>=14.0

# --- Makine Öğrenmesi ---
scikit-learn>=1.3
xgboost<2.0
imbalanced-learn>=0.11

# --- XAI ---
shap>=0.44
lime>=0.2

# --- LLM ---
langchain>=0.3
langchain-openai>=0.2
langchain-community>=0.3
langchain-core>=0.3
langchain-ollama
pydantic>=2.5
tiktoken

# --- RAG ---
langchain-chroma
langchain-huggingface
langchain-text-splitters
sentence-transformers
chromadb

# --- Görselleştirme ---
matplotlib>=3.7
seaborn>=0.12
plotly>=5.0

# --- Arayüz ---
streamlit>=1.30
jinja2>=3.1

# --- API ---
fastapi
uvicorn[standard]

# --- Utility ---
joblib>=1.3
python-dotenv>=1.0
loguru>=0.7
tqdm>=4.65
requests>=2.31
